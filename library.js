// cria uma variável e importa todas as fuctions do documento em './js/matriz'
const matriz = require('./js/matriz');

// cria uma variável e importa todas as fuctions do documento em './js/tabela'
const table = require('./js/tabela')

// cria uma variável e importa todas as fuctions do documento em './js/vet'
const vet = require('./js/vet');

// cria uma variável e importa todas as fuctions do documento em './js/vet'
const Calcula = require('./js/math')

module.exports={
    matriz:matriz,
    table:table,
    vet:vet,
    Calcula:Calcula,
}